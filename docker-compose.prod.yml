# version: '3'
# services:
#   db:
#     image: mysql:5.7
#     environment:
#       MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
#     ports:
#       - "${DB_PORT}:3306"
#     volumes:
#       - db-data:/var/lib/mysql
#   app:
#     image: your-docker-image-name
#     environment:
#       RAILS_ENV: production
#       RAILS_SERVE_STATIC_FILES: 'true'
#       DB_USER: ${DB_USER}
#       DB_PASSWORD: ${DB_PASSWORD}
#       DB_HOST: ${DB_HOST}
#       FRONTEND_ORIGIN: ${FRONTEND_ORIGIN}
#     ports:
#       - "80:3000"
#     depends_on:
#       - db
# volumes:
#   db-data:

version: '3'
services:
  # db:
  #   image: mysql:5.7
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
  #   ports:
  #     - "${DB_PORT}:3306"
  #   volumes:
  #     - db-data:/var/lib/mysql
  app:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    environment:
      RAILS_ENV: production
      RAILS_SERVE_STATIC_FILES: 'true'
      # DATABASE_URL: mysql2://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/your_db_name
    ports:
      - "80:3000"
#     depends_on:
#       - db
# volumes:
#   db-data: